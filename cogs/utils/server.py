import aiosqlite
# TODO: replace all non-async messages with attributes
class server:
    def __init__(self, bot): #init func is useless because it cant be async Pepepains
        self.bot = bot

# this is empty, because servers are going bye bye for the time being
# as an attempt towards a more classic and original fishy bot